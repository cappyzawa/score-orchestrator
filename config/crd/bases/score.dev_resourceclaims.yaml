---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: resourceclaims.score.dev
spec:
  group: score.dev
  names:
    kind: ResourceClaim
    listKind: ResourceClaimList
    plural: resourceclaims
    shortNames:
    - rc
    singular: resourceclaim
  scope: Namespaced
  versions:
  - name: v1b1
    schema:
      openAPIV3Schema:
        description: |-
          ResourceClaim represents a single resource dependency resolution contract.
          Provisioners drive it to Bound and publish standardized outputs for consumption by runtimes.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceClaimSpec declares the requested dependency and parameters
              the resolver needs.
            properties:
              class:
                description: Class optionally selects a resolver subclass/plan (PF-specific).
                type: string
              deprovisionPolicy:
                description: DeprovisionPolicy controls lifecycle of provisioned resources
                  when unbound.
                enum:
                - Delete
                - Retain
                - Orphan
                type: string
              id:
                description: ID optionally pins to an existing resource instance (resolver
                  decides semantics).
                type: string
              key:
                description: Key is the logical key under Workload.spec.resources
                  (e.g., "db", "cache").
                type: string
              params:
                description: Params are resolver-specific inputs (opaque to the orchestrator/runtime).
                x-kubernetes-preserve-unknown-fields: true
              type:
                description: Type is an abstract resource type (e.g., "postgresql",
                  "redis", "s3-bucket").
                type: string
              workloadRef:
                description: WorkloadRef points back to the owning Workload.
                properties:
                  name:
                    description: Name is the object name.
                    type: string
                  namespace:
                    description: Namespace is the object namespace.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - key
            - type
            - workloadRef
            type: object
          status:
            description: ResourceClaimStatus is written by resolvers to report progress
              and outputs.
            properties:
              lastTransitionTime:
                description: LastTransitionTime records when the phase last changed.
                format: date-time
                type: string
              message:
                description: Message is a short, single-sentence human message aligned
                  with the reason.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last reconciled spec generation.
                format: int64
                type: integer
              outputs:
                description: Outputs are standardized resolver outputs for consumption
                  by runtimes.
                properties:
                  cert:
                    description: Cert provides certificate/key material or a reference
                      to it.
                    properties:
                      data:
                        additionalProperties:
                          format: byte
                          type: string
                        description: Data allows inlined certificate/key material
                          (base64-encoded in JSON).
                        type: object
                      secretName:
                        description: SecretName optionally points to a Secret containing
                          certificate/key data.
                        type: string
                    type: object
                  configMapRef:
                    description: ConfigMapRef points to a ConfigMap containing configuration
                      data.
                    properties:
                      name:
                        description: Name is the object name (namespace is implicit
                          from the binding).
                        type: string
                    required:
                    - name
                    type: object
                  image:
                    description: Image exposes container image reference for image-based
                      resources.
                    type: string
                  secretRef:
                    description: SecretRef points to a Secret containing credentials
                      or connection data.
                    properties:
                      name:
                        description: Name is the object name (namespace is implicit
                          from the binding).
                        type: string
                    required:
                    - name
                    type: object
                  uri:
                    description: URI exposes a connection endpoint (e.g., jdbc:, redis:,
                      https:).
                    type: string
                type: object
                x-kubernetes-validations:
                - message: at least one of secretRef|configMapRef|uri|image|cert must
                    be set
                  rule: has(self.secretRef) || has(self.configMapRef) || has(self.uri)
                    || has(self.image) || has(self.cert)
              outputsAvailable:
                description: OutputsAvailable indicates whether outputs are ready
                  for consumption.
                type: boolean
              phase:
                description: Phase summarizes resolver progress (Pending/Binding/Bound/Failed).
                enum:
                - Pending
                - Binding
                - Bound
                - Failed
                type: string
              reason:
                description: Reason is an abstract machine-readable reason; avoid
                  runtime-specific nouns.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
