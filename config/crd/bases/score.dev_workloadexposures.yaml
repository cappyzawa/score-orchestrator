---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: workloadexposures.score.dev
spec:
  group: score.dev
  names:
    kind: WorkloadExposure
    listKind: WorkloadExposureList
    plural: workloadexposures
    singular: workloadexposure
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadRef.name
      name: Workload
      type: string
    - jsonPath: .spec.runtimeClass
      name: Runtime
      type: string
    - jsonPath: .status.exposures[0].url
      name: TopURL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1b1
    schema:
      openAPIV3Schema:
        description: |-
          WorkloadExposure represents runtime-specific endpoint exposure for a Workload.
          The Orchestrator creates and manages the spec, while Runtime Controllers update the status.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              WorkloadExposureSpec defines the desired state of WorkloadExposure.
              This is managed by the Orchestrator and specifies which Workload's endpoints should be exposed.
            properties:
              observedWorkloadGeneration:
                description: ObservedWorkloadGeneration is the generation of the Workload
                  used to compute this exposure.
                format: int64
                type: integer
              runtimeClass:
                description: RuntimeClass is the runtime controller class responsible
                  for materializing exposures.
                type: string
              workloadRef:
                description: WorkloadRef identifies the source Workload for endpoint
                  exposure.
                properties:
                  name:
                    description: Name is the name of the Workload.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the Workload.
                      If not specified, assumes the same namespace as the WorkloadExposure.
                    type: string
                  uid:
                    description: UID is the UID of the Workload for strong identity
                      across renames/recreates.
                    type: string
                required:
                - name
                type: object
            required:
            - observedWorkloadGeneration
            - runtimeClass
            - workloadRef
            type: object
          status:
            description: |-
              WorkloadExposureStatus defines the observed state of WorkloadExposure.
              This is managed by Runtime Controllers and reflects actual endpoint availability.
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the WorkloadExposure resource.
                  Standard condition types:
                  - "Ready": all exposures are ready and accessible
                  - "RuntimeReady": runtime is ready to handle exposures
                  - "ExposureReady": at least one exposure is available
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              exposures:
                description: |-
                  Exposures contains the list of actual exposed endpoints.
                  Exposures are ordered by descending priority; the first entry is mirrored
                  into Workload.status.endpoint by the Orchestrator.
                items:
                  description: ExposureEntry represents a single exposed endpoint.
                  properties:
                    name:
                      description: Name is a logical identifier for this exposure
                        (e.g., "web", "api", "metrics").
                      type: string
                    reachable:
                      description: Reachable indicates controller-side self-check
                        result, if any.
                      type: boolean
                    ready:
                      description: Ready indicates if this exposure is ready to serve
                        traffic.
                      type: boolean
                    schemeHint:
                      description: SchemeHint suggests protocol family for consumers
                        (HTTP, HTTPS, GRPC, TCP, OTHER).
                      type: string
                    scope:
                      description: Scope indicates exposure reachability (e.g., Public,
                        ClusterLocal, VPC).
                      type: string
                    type:
                      description: Type describes the exposure mechanism (e.g., "ingress",
                        "nodeport", "loadbalancer").
                      type: string
                    url:
                      description: URL is the accessible endpoint URL.
                      format: uri
                      type: string
                      x-kubernetes-validations:
                      - message: URL must start with http:// or https://
                        rule: self.startsWith('http://') || self.startsWith('https://')
                  required:
                  - ready
                  - url
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
